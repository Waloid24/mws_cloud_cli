# Cloud CLI for MWS Master’s Program Entrance

Small cloud-style CLI for managing local profiles.

## Build

To build the project use:

```bash
go build .
```

## Run

To run the project use:

```bash
go run . --help
```

or:

```bash
./mws_cloud_cli --help
```
